public enum ChallangeLevel{
    Easy,
    Normal,
    Hard
}
public enum SubtitlesMode { 
    On,
    Off
}
public enum GameHintMode { 
    On,
    Off
}
public enum TutorialsMode { 
    On,
    Off
}
public enum PhotoMode { 
    On,
    Off
}